subset:
  name: prior_reading__student__parent
  include_groups:
    - Baseline
    - pTarget
    - pParent
  exclude_groups: []
load:
  enabled: false # set to true to load existing results
  run_id: null   # e.g. "2026-01-22_121706__prior_reading__student__parent"
  # enabled: true # set to true to load existing results
  # run_id: 2026-01-26_100840__prior_reading__student__parent   